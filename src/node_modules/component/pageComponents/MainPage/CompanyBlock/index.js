import React from 'react'
import './CompanyBlock.sass'

export default function CompanyBlock(props) {
  return (
    <section className="companyBlock" id="company" ref={props.companyRef}>
      <div className="container">
        <h5 className="companyBlock__title">Company</h5>
        <p className="companyBlock__text">Staketab is a staking provider that champions credibility,
        individual approach and immediate expansion of new cryptos offered by the market.
        You will never be missed in huge dozens of crypto updates.
        We are refining the most profitable options for you and launching corresponding staking services,
        block explorers and data aggregators. Join us and enjoy staking profits!</p>
      </div>
    </section>
  )
}
