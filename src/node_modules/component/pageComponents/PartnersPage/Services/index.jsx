import React from 'react'
import './Services.sass'
import servicesData from 'component/pageComponents/PartnersPage/Services/servicesData'
import ServiceCard from 'component/pageComponents/PartnersPage/ServiceCard'

export default function Services() {
  return (
    <div className="partnersServices">
      <div className="container partnerscontainer">
        <div className="partnersServices__container">
          <h4 className="partnersServices__title">Partner Services</h4>
          <div className="partnersServices__row">
            {servicesData.map(elem => <div key={Math.random() * Math.pow(10, 6)} className="partnersServices__col"><ServiceCard img={elem.img} text={elem.text} /></div>)}
          </div>
        </div>
      </div>
    </div>
  )
}
